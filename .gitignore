# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon
























































*.quarantine# macOS quarantine files*.crash# macOS crash reports*.log# macOS system logs*.lock# macOS user settings.CFUserTextEncoding# macOS cache files.com.apple.timemachine.donotpresent# macOS Time Machine files.VolumeIcon.icns# macOS volume info.Network Trash Folder# macOS network stores.apdisk.TemporaryItems# macOS temporary files*.DS_Store# macOS Finder tags*.qlgenerator# macOS Quick Look files._*# macOS resource fork files.LSOverride*.AppleDouble# macOS metadata files.Trashes.Spotlight-V100._*.DS_Store?.DS_Store# macOS folder attributes.Trashes.Spotlight-V100# Files that might appear on external disk._*# Thumbnails